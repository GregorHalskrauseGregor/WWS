// 🧾 Leistungserfassung — Stub.
//
// Sobald er gebaut wird, ist er ein Vorgangs-Experte wie die anderen: schema
// (Kunde, Auftrag, Positionen mit Menge × Einzelpreis) plus finalisiere()
// (PDF/DOCX erzeugen). Die Sammel- und Nachfrage-Mechanik kommt vom Motor.

module.exports = {
  id: 'leistungserfassung',
  name: 'Leistungserfassung',
  emoji: '🧾',
  beschreibung: 'Erfasst erbrachte Leistungen zur Abrechnung (Positionen, Menge, Einzelpreis, Kunde). Noch nicht implementiert.',
  zustaendigWenn: 'Der Nutzer will erbrachte Leistungen abrechnen oder eine Rechnung erstellen.',
  implementiert: false
};
