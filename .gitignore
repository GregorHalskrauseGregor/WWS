node_modules/
.env

# User-Daten (Themen, Gedächtnis, Protokoll) — die gehören ins Railway-Volume,
# nicht ins Repo. Beim lokalen Entwickeln liegt das Verzeichnis trotzdem daneben,
# wird aber nicht committet.
data/

# Git-Aufraeumreste (entstehen nur auf gemounteten Laufwerken ohne Loeschrecht)
.git/stale/

# Arbeitsmaterial, gehoert nicht ins Repo (36 MB Katalog-Rohtexte)
Kataloge Shops/

# Umzugspaket, wird bei Bedarf neu erzeugt
wws-daten.zip
